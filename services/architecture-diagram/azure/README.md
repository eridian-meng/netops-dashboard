# Azure Architecture Diagram

Reserved for future Azure diagram scripts. Add a `main.py` or `run.py` here and map this folder through `scriptGroupPath`, or add a specific `scriptPath` in `catalog/services.json`.
